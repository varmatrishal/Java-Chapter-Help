import java.util.Date;/**   This class times the two loops given in Project 2.   @author Charles Hoot    @version 4.0 */public class TimedLoops {	public static void loopA(int n) 	{		long sum = 0;		for (int i = 1; i <= n; i++)			for (int j = 1; j <= 10000; j++)				sum = sum + j;	} // end loopA 	public static void loopB(int n) 	{		long sum = 0;		for (int i = 1; i <= n; i++)			for (int j = 1; j <= n; j++)				sum = sum + j;	} // end loopB 	public static void main(String[] args) 	{		long startTime, stopTime;		long elapsedTimeA = 0, elapsedTimeB = 0;		Date current;		boolean found = false;		int n = 10000;		while (!found && (n > 0))		{			System.out.println("Trying n = " + n);			// Loop A			current = new Date();                // Get current time			startTime = current.getTime();			loopA(n);                            // Execute Loop A 			current = new Date();                // Get current time			stopTime = current.getTime();			elapsedTimeA = stopTime - startTime; // In milliseconds			// Loop B			current = new Date();                // Get current time			startTime = current.getTime();			loopB(n);                            // Execute Loop B			current = new Date();                // Get current time			stopTime = current.getTime();			elapsedTimeB = stopTime - startTime; // In milliseconds			System.out.print  ("\tTime for loop A: " + elapsedTimeA);			System.out.println("\tTime for loop B: " + elapsedTimeB);			if (elapsedTimeB < elapsedTimeA) 				found = true;			else				n--;		} // end while		System.out.println("A value of n for which loop B is faster is " + n);	} // end main 	} // end TimedLoops /*Trying n = 10000	Time for loop A: 11	Time for loop B: 1A value of n for which loop B is faster is 10000 */